build:
  docker:
    web: Dockerfile
    worker: Dockerfile
run:
  release:
    image: worker
    command:
      - ./scripts/docgen.sh
  web: Run serve --environment $ENVIRONMENT --hostname 0.0.0.0 --port $PORT
